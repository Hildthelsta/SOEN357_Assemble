# SOEN357_Assemble

# Requirements

Node.js (18.0+)
Docker App


# Run MySQL

Open the Docker App
Input into terminal: run docker-compose.yml file(or 'docker compose up -d')

# Run DB

npm install mysql2 dotenv
node index.js

